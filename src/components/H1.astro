---
interface Props {
    dynamic?: boolean;
    color?: string;
    align?: string;
    style?: string;
}
const { dynamic = false, color, align, style } = Astro.props;
---

<h1 class={`${dynamic && "madri"}`} style={`color: ${color}; text-align: ${align}; ${style}`}>
    <slot />
</h1>

<style lang="scss">
    @use "~styles/fonts";

    h1 {
        @include fonts.grandstander;
        font-size: 2rem;
        margin: 0;
    }
</style>
